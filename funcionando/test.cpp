#include <iostream>
#include <cstdio>
#include "leitura.h"




int main() {
    double i, PC, AC = 0;
    // lerArquivo(i, PC, AC);
      int bin = decimalParaBinario(3);
    // int bin = decimalParaBinario(3);
    // int dec = binarioParaDecimal(bin);
    printf("%d\n",bin);


    return 0;
}
